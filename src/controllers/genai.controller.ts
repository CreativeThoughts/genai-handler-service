import { Request, Response } from "express"; import { generateAIResponse } from "../services/genai.service"; import { z } from "zod"; import { GeneratePromptSchema } from "../schemas/genai.schema"; type GeneratePromptRequest = z.infer<typeof GeneratePromptSchema>; export const generateResponse = async (req: Request, res: Response) => { try { const { prompt } = req.body as GeneratePromptRequest; const response = await generateAIResponse(prompt); return res.json({ response }); } catch (error) { console.error("Error generating response:", error); return res.status(500).json({ error: "Failed to generate response" }); } };
